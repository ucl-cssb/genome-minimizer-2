# genome-minimizer-2 — direct dependencies, pinned to tested versions.
#
# For the uv workflow, prefer `uv sync` (reads pyproject.toml). This file exists
# to support a plain `python -m venv` + pip install; see README.md for both
# paths. pip resolves the transitive dependencies of these packages.
pandas[pyarrow]==2.3.3
numpy==2.4.6
torch==2.12.0
scikit-learn==1.9.0
matplotlib==3.10.9
seaborn==0.13.2
scipy==1.17.1
biopython==1.87
wandb==0.27.0
huggingface-hub==1.17.0
marimo==0.23.8
nbformat==5.10.4
altair==6.1.0
polars==1.41.2
cobra==0.31.1
pyarrow==24.0.0
statsmodels==0.14.6
